# RELEASE

## Checklist
1. Ensure `make check` passes.
2. Update `docs/CHANGELOG.md` with shipped changes.
3. Bump version in `package.json`.
4. Tag release (e.g. `v0.1.0`).
5. Publish GitHub Release notes linking the tag.
