# CONTRIBUTING

## Workflow
1. Fork the repo and create a feature branch.
2. Run `make check` before opening a PR.
3. Describe the change and update `docs/CHANGELOG.md` if user-facing.

## Style
- Keep CSS tokens consistent with the theme system.
- Prefer small, composable components.
